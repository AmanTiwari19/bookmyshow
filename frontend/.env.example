# Local dev: leave this UNSET. The app falls back to "/api", which the Vite dev
# proxy (vite.config.js) forwards to the backend on http://localhost:4000.
#
# Production (Vercel): set this to your deployed backend's base URL, e.g.
# VITE_API_URL="https://bms-backend.onrender.com"
# VITE_API_URL=""
